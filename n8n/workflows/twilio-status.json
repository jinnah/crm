{
  "name": "Twilio Delivery Status",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "twilio-status",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "wh-twilio-status-post",
      "name": "Status Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        0
      ],
      "webhookId": "twilio-status-post"
    },
    {
      "parameters": {
        "jsCode": "// Twilio webhook authenticity: HMAC-SHA1 over the public URL plus the\n// alphabetically sorted POST parameters, keyed with the auth token.\nfunction twilioSignatureValid(params, headers, path) {\n  const authToken = $env.TWILIO_AUTH_TOKEN || '';\n  const url = ($env.TWILIO_WEBHOOK_BASE_URL || '') + '/webhook/' + path;\n  const sorted = Object.keys(params).sort().map((k) => k + params[k]).join('');\n  const provided = headers['x-twilio-signature'] || '';\n  if (!authToken || !provided) return false;\n  const expected = crypto.createHmac('sha1', authToken).update(url + sorted, 'utf8').digest('base64');\n  const a = Buffer.from(expected);\n  const b = Buffer.from(provided);\n  return a.length === b.length && crypto.timingSafeEqual(a, b);\n}\nconst crypto = require('crypto');\nconst item = $input.all()[0];\nconst params = item.json.body || {};\nconst headers = item.json.headers || {};\n\nif (!twilioSignatureValid(params, headers, 'twilio-status')) {\n  return [{ json: { reject: true, status: 403, message: 'invalid signature' } }];\n}\nconst sid = params.MessageSid || params.SmsSid;\nconst status = params.MessageStatus || params.SmsStatus;\nif (!sid || !status) {\n  return [{ json: { reject: true, status: 422, message: 'missing MessageSid or MessageStatus' } }];\n}\nreturn [{\n  json: {\n    reject: false,\n    payload: {\n      provider_sid: sid,\n      status,\n      error_code: params.ErrorCode ? String(params.ErrorCode).slice(0, 32) : null,\n      error_message: params.ErrorMessage ? String(params.ErrorMessage).slice(0, 300) : null,\n    },\n  },\n}];\n"
      },
      "id": "code-normalize",
      "name": "Normalize",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        0
      ]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json[\"reject\"] }}",
              "value2": true
            }
          ]
        }
      },
      "id": "if-reject?",
      "name": "Reject?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [
        440,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.CRM_API_URL }}/api/v1/inbound/message-status",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.CRM_INBOUND_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.payload) }}",
        "options": {
          "timeout": 15000,
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "id": "crm-status-write",
      "name": "CRM Status Update",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        0
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// Only a real CRM 2xx may become a 200 for Twilio. `matched: false` is a\n// normal 2xx result (an authenticated callback for a SID we do not track) and\n// counts as handled. Permanent 4xx are never retried; temporary problems are.\n// No CRM key, header or callback content is echoed.\nconst PERMANENT = [400, 401, 403, 404, 405, 409, 413, 415, 422];\n\nconst response = $input.all()[0].json;\nconst payload = $('Normalize').first().json.payload;\nconst status = Number(response.statusCode || 0);\n\nif (status >= 200 && status < 300) {\n  return [{ json: { outcome: 'success' } }];\n}\nif (PERMANENT.includes(status)) {\n  return [{ json: { outcome: 'permanent', status,\n    message: 'CRM rejected the status callback (' + status + ')' } }];\n}\nreturn [{ json: { outcome: 'retry', payload } }];\n"
      },
      "id": "code-classify-status",
      "name": "Classify Status",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        0
      ]
    },
    {
      "parameters": {
        "rules": {
          "values": [
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.outcome }}",
                    "rightValue": "success",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              },
              "outputKey": "success"
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.outcome }}",
                    "rightValue": "retry",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              },
              "outputKey": "retry"
            }
          ]
        },
        "options": {
          "fallbackOutput": "extra",
          "renameFallbackOutput": "failed"
        }
      },
      "id": "route-outcome",
      "name": "Route Outcome",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.2,
      "position": [
        1100,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: 'ok'}) }}",
        "options": {}
      },
      "id": "respond-status",
      "name": "Respond Status OK",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1760,
        -140
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.CRM_API_URL }}/api/v1/inbound/message-status",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.CRM_INBOUND_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.payload) }}",
        "options": {
          "timeout": 15000,
          "response": {
            "response": {
              "fullResponse": true
            }
          }
        }
      },
      "id": "crm-status-retry",
      "name": "CRM Status Retry",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1320,
        40
      ],
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 4,
      "waitBetweenTries": 2000
    },
    {
      "parameters": {
        "jsCode": "// Outcome of the bounded synchronous retries for the status callback.\nconst response = $input.all()[0].json;\nconst status = Number(response.statusCode || 0);\nif (status >= 200 && status < 300) {\n  return [{ json: { outcome: 'success' } }];\n}\nreturn [{ json: { outcome: 'exhausted', status: 503,\n  message: 'CRM unavailable after retries' } }];\n"
      },
      "id": "code-classify-status-retry",
      "name": "Classify Status Retry",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        40
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json.outcome === 'success' ? {status: 'ok'} : {error: $json.message}) }}",
        "options": {
          "responseCode": "={{ $json.outcome === 'success' ? 200 : 503 }}"
        }
      },
      "id": "respond-status-retry",
      "name": "Respond Status Retry Result",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1760,
        40
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({error: $json.message || 'rejected'}) }}",
        "options": {
          "responseCode": "={{ $json.status || 403 }}"
        }
      },
      "id": "respond-respond-status-failure",
      "name": "Respond Status Failure",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1760,
        220
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({error: $json.message || 'rejected'}) }}",
        "options": {
          "responseCode": "={{ $json.status || 403 }}"
        }
      },
      "id": "respond-respond-reject",
      "name": "Respond Reject",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        660,
        240
      ]
    }
  ],
  "connections": {
    "Status Webhook": {
      "main": [
        [
          {
            "node": "Normalize",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize": {
      "main": [
        [
          {
            "node": "Reject?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Reject?": {
      "main": [
        [
          {
            "node": "Respond Reject",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "CRM Status Update",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM Status Update": {
      "main": [
        [
          {
            "node": "Classify Status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify Status": {
      "main": [
        [
          {
            "node": "Route Outcome",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Route Outcome": {
      "main": [
        [
          {
            "node": "Respond Status OK",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "CRM Status Retry",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Status Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM Status Retry": {
      "main": [
        [
          {
            "node": "Classify Status Retry",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify Status Retry": {
      "main": [
        [
          {
            "node": "Respond Status Retry Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false,
    "saveExecutionProgress": false
  },
  "active": false,
  "pinData": {}
}
