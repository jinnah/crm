{
  "name": "Website Form Intake",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "web-form",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "wh-web-form-post",
      "name": "Form Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        0
      ],
      "webhookId": "web-form-post"
    },
    {
      "parameters": {
        "jsCode": "// Validate and normalize a website-form submission into a CRM inbound event.\nconst item = $input.all()[0];\nconst body = item.json.body || {};\nconst headers = item.json.headers || {};\n\n// Lightweight abuse protection: optional shared secret plus a honeypot field.\nconst sharedSecret = $env.FORM_SHARED_SECRET || '';\nif (sharedSecret && headers['x-form-secret'] !== sharedSecret) {\n  return [{ json: { reject: true, status: 403, message: 'forbidden' } }];\n}\nif (body.website) {\n  // Honeypot filled: acknowledge quietly, never write to the CRM.\n  return [{ json: { reject: true, status: 200, message: 'accepted' } }];\n}\n\nconst submissionId = String(body.submission_id || '').trim();\nconst name = String(body.name || '').trim();\nconst email = String(body.email || '').trim();\nconst phone = String(body.phone || '').trim();\nconst message = String(body.message || '').trim();\nif (!submissionId || !message || (!email && !phone)) {\n  return [{ json: { reject: true, status: 422, message: 'submission_id, message and email or phone are required' } }];\n}\nif (message.length > 10000 || name.length > 200 || email.length > 320 || phone.length > 50) {\n  return [{ json: { reject: true, status: 413, message: 'payload too large' } }];\n}\n\nconst metadata = {};\nfor (const key of ['form', 'page', 'campaign', 'referrer']) {\n  if (body[key]) metadata[key] = String(body[key]).slice(0, 500);\n}\n\nreturn [{\n  json: {\n    reject: false,\n    idempotency_key: 'form:' + submissionId,\n    event: {\n      channel: 'web_form',\n      provider: 'website',\n      external_event_id: submissionId,\n      event_type: 'form_submission',\n      sender_name: name || null,\n      sender_email: email || null,\n      sender_phone: phone || null,\n      subject: body.subject ? String(body.subject).slice(0, 500) : null,\n      content: message,\n      received_at: body.submitted_at || new Date().toISOString(),\n      metadata,\n    },\n  },\n}];\n"
      },
      "id": "code-normalize",
      "name": "Normalize",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        0
      ]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json[\"reject\"] }}",
              "value2": true
            }
          ]
        }
      },
      "id": "if-reject?",
      "name": "Reject?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [
        440,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.all()[0].json;\nreturn [{ json: { event: item.event, idempotency_key: item.idempotency_key } }];\n"
      },
      "id": "code-prepare-attempt",
      "name": "Prepare Attempt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.CRM_API_URL }}/api/v1/inbound/events",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.CRM_INBOUND_API_KEY }}"
            },
            {
              "name": "Idempotency-Key",
              "value": "={{ $json.idempotency_key }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.event) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "timeout": 20000
        }
      },
      "id": "crm-write",
      "name": "CRM Write",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        840,
        0
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// Selective retry: network errors, timeouts, 429 and 5xx are temporary and\n// worth retrying; 4xx validation/authentication failures are permanent and\n// must never be retried. Only non-personal fields are emitted.\nconst PERMANENT = [400, 401, 403, 404, 405, 409, 413, 415, 422];\n\nconst response = $input.all()[0].json;\nconst request = $('Prepare Attempt').first().json;\nconst status = Number(response.statusCode || 0);\nconst carry = { event: request.event, idempotency_key: request.idempotency_key };\n\nif (status >= 200 && status < 300) {\n  const body = response.body || {};\n  return [{ json: { ...carry, outcome: 'success', lead_id: body.lead_id,\n    activity_id: body.activity_id, replayed: body.replayed === true } }];\n}\nif (PERMANENT.includes(status)) {\n  return [{ json: { ...carry, outcome: 'permanent', status,\n    message: 'CRM rejected the request (' + status + ')' } }];\n}\nreturn [{ json: { ...carry, outcome: 'retry', status } }];\n"
      },
      "id": "code-classify-response",
      "name": "Classify Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1040,
        0
      ]
    },
    {
      "parameters": {
        "rules": {
          "values": [
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.outcome }}",
                    "rightValue": "success",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              },
              "outputKey": "success"
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.outcome }}",
                    "rightValue": "retry",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              },
              "outputKey": "retry"
            }
          ]
        },
        "options": {
          "fallbackOutput": "extra",
          "renameFallbackOutput": "failed"
        }
      },
      "id": "route-outcome",
      "name": "Route Outcome",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.2,
      "position": [
        1240,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: 'ok', lead_id: $json.lead_id, activity_id: $json.activity_id, replayed: $json.replayed}) }}"
      },
      "id": "respond-ok",
      "name": "Respond OK",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1900,
        -140
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.CRM_API_URL }}/api/v1/inbound/events",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.CRM_INBOUND_API_KEY }}"
            },
            {
              "name": "Idempotency-Key",
              "value": "={{ $json.idempotency_key }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.event) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true
            }
          },
          "timeout": 20000
        }
      },
      "id": "crm-write-retry",
      "name": "CRM Write Retry",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1460,
        40
      ],
      "retryOnFail": true,
      "maxTries": 4,
      "waitBetweenTries": 2000,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// Outcome of the bounded synchronous retries. No personal data is emitted.\nconst response = $input.all()[0].json;\nconst status = Number(response.statusCode || 0);\nif (status >= 200 && status < 300) {\n  const body = response.body || {};\n  return [{ json: { outcome: 'success', lead_id: body.lead_id,\n    activity_id: body.activity_id, replayed: body.replayed === true } }];\n}\nreturn [{ json: { outcome: 'exhausted', status: 503,\n  message: 'CRM unavailable after retries' } }];\n"
      },
      "id": "code-classify-retry",
      "name": "Classify Retry",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1680,
        40
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json.outcome === 'success' ? {status: 'ok', lead_id: $json.lead_id, activity_id: $json.activity_id, replayed: $json.replayed} : {error: $json.message}) }}",
        "options": {
          "responseCode": "={{ $json.outcome === 'success' ? 200 : 503 }}"
        }
      },
      "id": "respond-retry-result",
      "name": "Respond Retry Result",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1900,
        40
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({error: $json.message || 'rejected'}) }}",
        "options": {
          "responseCode": "={{ $json.status || 403 }}"
        }
      },
      "id": "respond-respond-failure",
      "name": "Respond Failure",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1900,
        220
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({error: $json.message || 'rejected'}) }}",
        "options": {
          "responseCode": "={{ $json.status || 403 }}"
        }
      },
      "id": "respond-respond-reject",
      "name": "Respond Reject",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        640,
        240
      ]
    }
  ],
  "connections": {
    "Form Webhook": {
      "main": [
        [
          {
            "node": "Normalize",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize": {
      "main": [
        [
          {
            "node": "Reject?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Reject?": {
      "main": [
        [
          {
            "node": "Respond Reject",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Prepare Attempt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Attempt": {
      "main": [
        [
          {
            "node": "CRM Write",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM Write": {
      "main": [
        [
          {
            "node": "Classify Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify Response": {
      "main": [
        [
          {
            "node": "Route Outcome",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Route Outcome": {
      "main": [
        [
          {
            "node": "Respond OK",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "CRM Write Retry",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM Write Retry": {
      "main": [
        [
          {
            "node": "Classify Retry",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify Retry": {
      "main": [
        [
          {
            "node": "Respond Retry Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false,
    "saveExecutionProgress": false
  },
  "active": false,
  "pinData": {}
}
